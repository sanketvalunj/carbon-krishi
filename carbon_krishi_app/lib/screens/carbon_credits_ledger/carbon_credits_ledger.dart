import 'package:flutter/material.dart';

class CarbonCreditsLedger extends StatelessWidget {
  const CarbonCreditsLedger({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: const Text('Carbon Credits Ledger'),
      ),
      body: const Center(
        child: Text('Carbon Credits Ledger'),
      ),
    );
  }
}