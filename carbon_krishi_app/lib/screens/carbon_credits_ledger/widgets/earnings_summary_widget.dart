
import 'package:flutter/material.dart';

class EarningsSummaryWidget extends StatelessWidget {
  const EarningsSummaryWidget({super.key});

  @override
  Widget build(BuildContext context) {
    return Container(
      child: const Center(
        child: Text('Earnings Summary Widget'),
      ),
    );
  }
}
